# name: Deploy Stage

# on:
#   push:
#     branches: ["main"]
#     paths: ["app/**", ".github/workflows/**"]

# jobs:
#   lint:
#     uses: ./.github/workflows/jobs/lint.yml

#   test:
#     uses: ./.github/workflows/jobs/test.yml

#   deploy:
#     needs: [lint, test]
#     uses: ./.github/workflows/jobs/deploy.yml
#     with:
#       environment_name: staging
#       directory: stage
#       compose_file: docker-compose.stage.yml
#       bot_link: https://t.me/calendar_collection_stage_bot
#       environment_display_name: Stage

#   test-staging:
#     needs: [deploy]
#     runs-on: ubuntu-latest
#     steps:
#       - name: Test staging placeholder
#         run: |
#           echo "⚠️ Тесты для staging окружения не могут быть реализованы сейчас и будут сделаны в будущем"
#           echo "## Test Staging Status" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "⚠️ **Тесты для staging окружения не могут быть реализованы сейчас и будут сделаны в будущем**" >> $GITHUB_STEP_SUMMARY
